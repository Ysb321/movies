# Get a free key at https://www.themoviedb.org/settings/api
# Used by the backend proxy (/api/tmdb/*)
TMDB_API_KEY=your_tmdb_v3_api_key

# Same key, exposed to the browser as a fallback when the backend proxy
# cannot reach TMDB (e.g. static hosting without server functions).
NEXT_PUBLIC_TMDB_API_KEY=your_tmdb_v3_api_key

# Optional — Server 18 (Licensed Anime). With a YouTube Data API v3 key
# the lane resolves episodes via the documented search.list endpoint;
# without one it falls back to parsing the channel search page.
# Get one at https://console.cloud.google.com/apis/library/youtube.googleapis.com
YOUTUBE_API_KEY=

# Optional — extra OFFICIAL licensor channels for Server 18, as
# "Name:ChannelId" pairs. Only add channels operated by the rightsholder
# or their appointed licensee. See docs/licensed-anime.md.
LICENSED_ANIME_CHANNELS=

# Optional — Server 20 (8StreamApi). Set to your deployed 8StreamApi instance URL.
# Deploy from: https://github.com/himanshu8443/8StreamApi
# Example: https://your-8stream-api.vercel.app/api/v1
EIGHTSTREAM_API_URL=

# Optional — Server 21 (ScarperApi). Set to your deployed ScarperApi instance URL.
# Set SCARPER_API_KEY to your API key from the ScarperApi dashboard.
# Deploy from: https://github.com/junioralive/ScarperApi
# Example: https://your-scarper-api.vercel.app/api
SCARPER_API_URL=
SCARPER_API_KEY=

# Optional — Server 22 (P.R. Movies). Embed-based Hindi dubbed movies/series.
# Uses Vidsrc-compatible embed player (vid-src.top) for responsive 1080p/720p streams.
# No configuration needed - automatically uses vid-src.top embed URLs.
